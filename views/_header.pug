HEADER
    header.header
        nav.nav.nav--tours
            a.nav__el(href="/") All tours  
        .header__logo 
            img(src="/img/logo-white.png" alt ="Natours logo")
        nav.nav.nav__user 
            //- a.nav__el(href="#") My bookings 
            //- a.nav_el(href="#")
            //-     img.nav_user-img(src="img/user.jpg" alt="User photo")
            //-     span Nandan
            if user 
                a.nav__el.nav__el--logout(href="") Log out
                a.nav_el(href="#")
                    img.nav_user-img(src=`img/users/${user.photo}` alt=`Photo of ${user.name}`)
                    span= user.name.split(' ')[0]
            else
                a.nav__el(href='/login') Log in
                a.nav__el.nav__el--cta(href='#') Sign up